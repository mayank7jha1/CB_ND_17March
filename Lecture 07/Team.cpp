#include<iostream>
using  namespace std;

int main() {
	int n;
	cin >> n;

	int i = 1;
	int count = 0;

	while (i <= n) {
		z
		int a, b, c;
		cin >> a >> b >> c;
		int sum = a + b + c;

		if (sum >= 2) {
			count = count + 1;
		}
		i = i + 1;
	}

	cout << count << endl;
}