#include<iostream>
#include<vector>
using  namespace std;

int main() {
	/*
		//Static Array:
		int a[] {4, 3, 2, 1, 6};
		int c[] = {1, 2, 3, 4, 5};
		int d[5] = {1, 2, 2, 5};
		int e[3] {1, 2, 2};
		//or Take input from the user.

		//Dynamic Array
		int *b = new int[] {5, 4, 3, 21, 9, 8};
		//or Take input from the user.

		//Vector:
		vector<int>v;
		vector<int>v2(10);
		vector<int>v1(10, 4);
		vector<int>v3{1, 2, 3, 4, 5};


		//Generic Rules:

		//Any container can be copied directly into another
		//container of the same type.

		//You can use any container as a data type.

		vector<int>v6 = v3;
		vector<int>v7 = {4, 55, 6, 6, 7, 8};

		//Vector can be printed in 3 ways:


		//1. Normal For loop:

		for (int i = 0; i < v3.size(); i = i + 1) {
			cout << v3[i] << " ";
		}

		cout << endl;


		//2. For each Loop:
		for (auto x : v3) {
			cout << x << " ";
		}
		cout << endl;


		//3. Using Iterators:
		for (auto x = v3.begin(); x != v3.end(); x = x + 1) {
			cout << *x << " ";
		}
		cout << endl;


		pair<int, int>p1;
		pair<int, int>p2 = p1;
		pair<int, int>p3 = make_pair(23, 54);
		pair<int, int>p4 = {432, 111};

	*/
	/*
		This is an array of data type pair that means
		every element of this array will be a pair.

		cout<<p5[i]<<endl; this will result in error?

		because p5[i] is a pair and if you  want to print a
		pair batao ki konsa element prrint karrna hain
		first or second.

		similarly be careful while taking input.

	*/

	// pair<int, int>p5[10];

	// pair<string, int>p6[10];

	// int n;
	// cin >> n;

	// for (int i = 0; i < n; i = i + 1) {
	// 	//cin >> p6[i].first >> p6[i].second;

	// 	string s;
	// 	int x;
	// 	cin >> s >> x;

	// 	// p6[i].first = s;
	// 	// p6[i].second = x;


	// 	//p6[i] = make_pair(s, x);
	// 	//p6[i] = {s, x};
	// }


	/*

		This is an array of vectors:

		every element of this array will be a
		vector.

		a[i] is a vector.


		You don't need to specify the column
		size here and it can be of variable length

	*/


	// int n;
	// cin >> n;
	// vector<int>a[n];

	// cout<<a[i](X)

	//Vector ke ander pair use karlena.
	//pair and ander vector use karlena.


}





















